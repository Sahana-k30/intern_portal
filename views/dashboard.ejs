<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= intern.name %>'s Dashboard</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f0f4f8;
            color: #1e293b;
            margin: 0;
            padding: 2rem;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
        }
        header {
            background-color: #ffffff;
            padding: 1.5rem;
            border-radius: 1rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
        }
        header h1 {
            font-size: 2rem;
            font-weight: 700;
            margin: 0;
            color: #334155;
        }
        .header-links a {
            color: #64748b;
            text-decoration: none;
            font-weight: 600;
            margin-left: 1.5rem;
            padding: 0.5rem 1rem;
            border-radius: 0.5rem;
            transition: background-color 0.2s;
        }
        .header-links a:hover {
            background-color: #e2e8f0;
        }
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
        }
        .card {
            background-color: #ffffff;
            padding: 1.5rem;
            border-radius: 1rem;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
        }
        .card h2 {
            font-size: 1.25rem;
            font-weight: 600;
            margin-top: 0;
            margin-bottom: 1rem;
            color: #334155;
        }
        .metric-value {
            font-size: 2.5rem;
            font-weight: 700;
            color: #3b82f6;
        }
        .referral-code {
            background-color: #f0f4f8;
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            font-size: 1.125rem;
            font-family: monospace;
            color: #334155;
        }
        .rewards-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .rewards-list li {
            background-color: #f8fafc;
            border-left: 4px solid #64748b;
            padding: 0.75rem;
            border-radius: 0.25rem;
            margin-bottom: 0.5rem;
            font-weight: 500;
            display: flex;
            align-items: center;
        }
        .rewards-list li.unlocked {
            border-left-color: #22c55e;
            color: #16a34a;
        }
        .rewards-list li.unlocked::before {
            content: '✓';
            color: #22c55e;
            font-weight: 700;
            margin-right: 0.75rem;
        }
        .rewards-list li:not(.unlocked)::before {
            content: '☐';
            color: #64748b;
            font-weight: 700;
            margin-right: 0.75rem;
        }
        @media (max-width: 768px) {
            header {
                flex-direction: column;
                text-align: center;
            }
            .header-links {
                margin-top: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Hello, <%= intern.name %>!</h1>
            <div class="header-links">
                <a href="/leaderboard">Leaderboard</a>
            </div>
        </header>

        <main class="dashboard-grid">
            <div class="card">
                <h2>Total Donations Raised</h2>
                <div class="metric-value">$<%= intern.donation %></div>
            </div>

            <div class="card">
                <h2>Your Referral Code</h2>
                <div class="referral-code"><%= intern.code %></div>
            </div>

            <div class="card">
                <h2>Rewards & Unlockables</h2>
                <ul class="rewards-list">
                    <li class="unlocked">Raise $500: Intern Badge</li>
                    <li class="unlocked">Raise $1000: Swag Bag</li>
                    <li class="unlocked">Raise $2000: Lunch with Manager</li>
                    <li>Raise $5000: Mentor Session with CEO</li>
                </ul>
            </div>
        </main>
    </div>
</body>
</html>